services:
  authentik-server:
    platform: linux/amd64
    build: docker/authentik-server/
    restart: always
  authentik-ldap:
    platform: linux/amd64
    build: docker/authentik-ldap/
    restart: always
  scim:
    platform: linux/amd64
    build: docker/scim/
